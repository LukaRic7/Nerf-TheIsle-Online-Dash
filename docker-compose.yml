version: '3.8'

services:
  isle-server:
    build:
      context: ./server
      dockerfile: Dockerfile
    environment:
      - PORT=56550
      - PYTHONUNBUFFERED=1
    ports:
      - "56550:56550"
    restart: unless-stopped
